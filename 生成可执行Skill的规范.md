# Skill：生成可执行 Skill 的规范

## 目标
当需要编写、整理或重构一个 Skill 时，必须优先产出“可执行、可复用、可识别模式”的规则文档。  
不要把 Skill 写成经验随笔、说明文章或概念堆砌。  
Skill 的核心不是信息多，而是让 Agent 能稳定识别“必须做什么、何时做、如何做、做到什么算完成”。

---

## 必做事项
每次生成 Skill 时，必须按以下顺序组织内容：

1. 先明确这个 Skill 的目标和适用范围
2. 先写“必须做什么”
3. 再写“在什么条件下怎么做”
4. 同类规则保持统一表达格式
5. 把规则写成可执行动作，而不是抽象建议
6. 给出明确的成功标准和禁止事项
7. 删除无助于执行的冗余背景信息

---

## Skill 编写规则

### 1. 先定义目标，不要一开始就堆规则
生成 Skill 时，首先必须说明：

- 这个 Skill 是为了解决什么问题
- 它适用于什么场景
- 它不适用于什么场景
- 执行这个 Skill 的核心结果是什么

目标必须具体，不能只写成：

- “提高质量”
- “增强准确性”
- “优化流程”

必须写成 Agent 能理解的结果导向表述，例如：

- “在修改页面前准确定位目标元素，再执行最小范围修改”
- “在生成代码前先确认输入输出、依赖和边界条件”
- “在回复用户前先提取任务意图，再决定输出格式和细节层级”

---

### 2. 先写“必须做什么”，再写“在什么条件下怎么做”
Skill 中必须先出现主动作，再出现条件分支。  
不要一开始就写复杂条件，否则 Agent 容易把规则理解成“只有某些情况才需要执行”。

正确结构应为：

1. 先定义必须执行的动作
2. 再补充条件、例外、分支处理
3. 最后补充特殊情况和降级方案

推荐写法：

- “必须先确认目标元素，再执行修改”
- “必须先检查输入参数，再调用工具”
- “必须先判断用户意图，再生成回复”

然后再写：

- “如果存在多个相似元素，则先消歧再修改”
- “如果参数缺失，则先依据上下文补全或降级处理”
- “如果用户要求格式固定，则优先服从格式要求”

禁止把关键动作埋在条件句后面，例如：

- “如果用户提供了更多信息，则可以……”
- “在某些情况下，需要……”
- “必要时可考虑……”

这类写法容易让规则失去“必须执行”的力度。

---

### 3. 同类规则必须统一格式
同一个 Skill 内部，同类型规则必须尽量使用统一结构。  
这样 Agent 更容易从文档中抽取稳定模式，而不是把每一条规则都当成孤立例外。

统一的内容包括：

- 标题层级
- 动词开头方式
- 列表表达方式
- 条件分支写法
- 成功标准写法
- 禁止项写法

例如，同类动作建议都写成：

- “必须……”
- “优先……”
- “禁止……”
- “如果……则……”
- “完成后必须验证……”

而不要混用成：

- “建议考虑……”
- “可以适当……”
- “通常来说……”
- “在某些时候可能需要……”

如果规则强度不同，也要明确区分：

- **必须**：不可跳过
- **优先**：默认采用，除非有更强依据
- **可选**：仅在特定情况下使用
- **禁止**：明确不能做

---

### 4. 规则必须写成动作，而不是概念
Skill 不是知识笔记，也不是理念说明。  
每条规则都必须能回答：

- Agent 要做什么？
- 什么时候做？
- 依据什么做？
- 做完如何判断？

优先写动作型表述，例如：

- “收集所有候选元素，并按稳定属性排序”
- “先读取用户约束，再决定输出格式”
- “修改后复检目标是否正确命中”
- “调用工具前确认输入字段齐全”

避免只写概念型表述，例如：

- “要注意上下文”
- “要提高准确率”
- “要考虑可维护性”
- “要结合实际情况判断”

概念可以保留，但必须落到操作上。

---

### 5. 条件分支必须服务于主动作
条件分支的作用是帮助主动作更稳定执行，而不是把主动作冲淡。  
因此每个条件分支都必须依附于一个明确的主动作。

推荐结构：

- 主动作
  - 默认情况怎么做
  - 特殊情况怎么做
  - 无法确认时怎么降级
  - 完成后怎么验证

例如：

- 必须先定位目标元素
  - 默认优先使用稳定属性
  - 若有多个候选，则结合上下文消歧
  - 若定位信心不足，则继续检查，不要猜
  - 修改后必须复检

这样写，Agent 更容易把整段理解成一个动作闭环。

---

### 6. Skill 要追求“可执行性”，不是“信息密度”
Skill 中不应无节制增加背景、理念、延伸讨论。  
只保留那些会直接影响执行结果的内容。

应保留的内容：

- 明确动作
- 条件分支
- 决策依据
- 验证方法
- 反例
- 成功标准

应压缩或删除的内容：

- 过长背景介绍
- 与执行无关的理论讨论
- 重复表述
- 没有操作价值的抽象原则
- 不能影响结果的泛泛建议

判断标准不是“写得多不多”，而是：

**这段内容能否提高 Agent 的实际执行稳定性？**

如果不能，就不应放进核心 Skill。

---

### 7. 必须写清优先级和判断依据
Skill 中如果存在多个可能策略，必须写清优先级。  
否则 Agent 会把多个规则并列处理，导致执行不稳定。

例如不要只写：

- 使用稳定选择器
- 结合文本判断
- 看结构关系

而应写成：

1. 优先使用 `data-*` 属性
2. 其次使用稳定 `id`
3. 再使用语义属性
4. 之后才考虑结构定位
5. 文本匹配和位置匹配仅作辅助

只要出现“多个都可以”的情况，就要尽量转成“默认优先级”。

---

### 8. 必须有反例和禁止项
一个好的 Skill 不仅说明“该怎么做”，还要说明“不能怎么做”。  
禁止项能帮助 Agent 排除常见误区，减少误执行。

禁止项应尽量具体，例如：

- 禁止只靠重复文本定位元素
- 禁止因为是第一个候选项就直接修改
- 禁止为小改动重写整个区块
- 禁止用模糊建议替代明确动作
- 禁止把关键规则埋在例外条件中

不要只写成空泛表述，例如：

- “避免错误”
- “避免不合理情况”
- “注意边界”

---

### 9. 必须定义成功标准
Skill 的末尾必须给出“做到什么算成功”。  
否则 Agent 虽然执行了动作，但无法判断结果是否达标。

成功标准通常应包括：

- 是否命中正确目标
- 是否按预期完成动作
- 是否没有误伤其他部分
- 是否保留原有结构和约束
- 是否可被重复执行
- 是否在同类场景下保持一致

成功标准必须可验证，而不是价值判断式措辞。

---

## 推荐结构模板

### 1. 标题
直接说明 Skill 名称，不要过度抽象。

### 2. 目标
说明这个 Skill 解决什么问题、适用于什么任务。

### 3. 必做事项
先列出高层动作顺序。

### 4. 核心规则
按统一格式写：
- 必须做什么
- 优先怎么做
- 如果出现某条件怎么做
- 禁止怎么做

### 5. 标准执行流程
按步骤写出实际操作顺序。

### 6. 验证规则
说明完成后如何检查结果。

### 7. 反例
说明常见误区和禁止行为。

### 8. 成功标准
定义什么叫真正完成。

---

## 标准表达约定

### 应优先使用的表达
- 必须……
- 优先……
- 如果……则……
- 当……时，必须……
- 完成后必须验证……
- 禁止……

### 应减少使用的表达
- 建议……
- 可以考虑……
- 视情况而定……
- 通常来说……
- 尽量……
- 适当……

如果确实要保留弹性，也应明确弹性的边界，而不是让规则变模糊。

---

## 生成 Skill 时的内部检查问题
在产出一个 Skill 之前，必须先自查：

- 这个 Skill 的目标是否足够具体？
- 规则是否先写了必须动作，再写条件分支？
- 同类规则的格式是否统一？
- 每条规则是否都能直接执行？
- 是否写清了优先级？
- 是否包含禁止项？
- 是否定义了成功标准？
- 是否删除了不影响执行的冗余内容？

只要以上任一问题答案不明确，就说明 Skill 还不够稳定。

---

## 反例
以下都不是好的 Skill 写法：

- 只有理念，没有动作
- 只有条件，没有主动作
- 同类规则格式混乱
- 规则强弱不分
- 大量背景说明，却没有执行步骤
- 没有验证环节
- 没有成功标准
- 把“更多上下文”误当成“更强 Skill”

---

## 成功标准
一个合格的 Skill，必须同时满足：

- 目标清晰
- 主动作明确
- 条件分支清楚
- 表达格式统一
- 可直接执行
- 有优先级
- 有验证方式
- 有禁止项
- 有成功标准
- 冗余上下文得到控制

一个优秀的 Skill，进一步要求：

- 在同类任务中可复用
- Agent 能稳定抽取模式
- 即使上下文较短，也能保持较高执行一致性
- 不依赖大量补充说明也能正确落地